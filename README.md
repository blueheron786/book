# Mwmbl Book

This repository hosts the [Mwmbl book](https://book.mwmbl.org/).

## Building

Requires hugo. Make sure you initialize submodules after cloning the repo by running `git submodule update --init`.